# Pyrus API Configuration

# Required: Your Pyrus login (email)
PYRUS_LOGIN=your-email@domain.com

# Required: Your Pyrus security key
# Get this from Pyrus Settings → Integrations → API
PYRUS_API_TOKEN=your-security-key-here

# Optional: Pyrus domain (defaults to pyrus.com)
# For custom installations, change this to your domain
PYRUS_DOMAIN=pyrus.com

# Optional: Override API base URL completely (overrides domain setting)
# Only use this if you need a completely custom URL
# PYRUS_BASE_URL=https://api.your-custom-domain.com/v4